# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Generated artifacts (never commit generated outputs)
artifacts/
data/interim/
data/processed/

# Cached external source snapshots (raw API responses, local StatsBomb clone,
# user-provided rankings/player data). Never committed; reproduced from source.
data/external/

# Environment / credentials (placeholders live in .env.example, which IS tracked)
.env
.env.local
